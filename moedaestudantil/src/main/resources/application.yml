spring:
  datasource:
    url: jdbc:h2:mem:moeda;MODE=MySQL;DB_CLOSE_DELAY=-1
    driver-class-name: org.h2.Driver

  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: false

  thymeleaf:
    cache: false

  h2:
    console:
      enabled: true

  mail:
    host: smtp.gmail.com
    port: 587
    username: sis.moedaestudantil@gmail.com
    password: SUA_SENHA_DE_APP_AQUI
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

server:
  port: 8080
